apiVersion: v1
kind: Service
metadata:
  labels:
    app: spring-ajar-rs
  name: spring-ajar-svc
  namespace: germany
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: spring-ajar-rs
  sessionAffinity: None
  type: NodePort
